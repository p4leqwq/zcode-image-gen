---
description: Generate image (grok-imagine-image / gpt-image-1)
---

Use the image-gen skill to generate an image from this request: $ARGUMENTS

Pick the provider the user asked for (default: grok), run the skill's script with a Bash timeout of at least 120000 ms, then show the resulting image path as a markdown link.
